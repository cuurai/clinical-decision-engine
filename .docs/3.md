Here’s an **extensive Decision Intelligence Domain** spec, following your REST pattern (`POST /entity`, `PATCH/DELETE /entity/:id`, `GET /entity`, `GET /entity/:id/...` for children).

---

## 1️⃣ Decision Session

**Description:** Logical container for all decision activity for a patient/context (e.g., per encounter, per CDS session).

**Operations**

- `GET   /api/v1/decision-sessions`
- `GET   /api/v1/decision-sessions/:id`
- `POST  /api/v1/decision-sessions`
- `PATCH /api/v1/decision-sessions/:id`
- `DELETE /api/v1/decision-sessions/:id`

**Child collections**

- `GET /api/v1/decision-sessions/:id/decision-requests`
- `GET /api/v1/decision-sessions/:id/decision-results`
- `GET /api/v1/decision-sessions/:id/risk-assessments`
- `GET /api/v1/decision-sessions/:id/alerts`
- `GET /api/v1/decision-sessions/:id/explanations`

---

## 2️⃣ Decision Request

**Description:** Immutable snapshot of input data when a decision is asked for (often maps to a CDS Hook invocation).

**Operations**

- `GET   /api/v1/decision-requests`
- `GET   /api/v1/decision-requests/:id`
- `POST  /api/v1/decision-requests` _(create-only)_

**Child collections**

- `GET /api/v1/decision-requests/:id/decision-results`
- `GET /api/v1/decision-requests/:id/explanations`

---

## 3️⃣ Decision Result

**Description:** Top-level outcome of a decision request, typically a bundle of cards/recommendations/scores.

**Operations**

- `GET   /api/v1/decision-results`
- `GET   /api/v1/decision-results/:id`
- `POST  /api/v1/decision-results`
- `PATCH /api/v1/decision-results/:id` _(e.g., update status)_
- `DELETE /api/v1/decision-results/:id`

**Child collections**

- `GET /api/v1/decision-results/:id/recommendations`
- `GET /api/v1/decision-results/:id/risk-assessments`
- `GET /api/v1/decision-results/:id/explanations`

---

## 4️⃣ Risk Assessment

**Description:** One or more risk scores/probabilities for a patient/context (e.g., Sepsis risk 0.78).

**Operations**

- `GET   /api/v1/risk-assessments`
- `GET   /api/v1/risk-assessments/:id`
- `POST  /api/v1/risk-assessments`
- `PATCH /api/v1/risk-assessments/:id`
- `DELETE /api/v1/risk-assessments/:id`

**Child collections**

- `GET /api/v1/risk-assessments/:id/explanations`

---

## 5️⃣ Recommendation (Clinical Recommendation Card)

**Description:** Individual suggestion (diagnostic, treatment, monitoring, follow-up, documentation, etc.).

**Operations**

- `GET   /api/v1/recommendations`
- `GET   /api/v1/recommendations/:id`
- `POST  /api/v1/recommendations`
- `PATCH /api/v1/recommendations/:id` _(e.g., clinician accepted/overridden/deferred)_
- `DELETE /api/v1/recommendations/:id`

**Child collections**

- `GET /api/v1/recommendations/:id/explanations`

---

## 6️⃣ Alert Evaluation

**Description:** Evaluation of a rule/model that may fire or suppress an alert, with reasons and severity.

**Operations**

- `GET   /api/v1/alert-evaluations`
- `GET   /api/v1/alert-evaluations/:id`
- `POST  /api/v1/alert-evaluations`
- `PATCH /api/v1/alert-evaluations/:id` _(e.g., snoozed, overridden)_
- `DELETE /api/v1/alert-evaluations/:id`

---

## 7️⃣ Explanation (Explanation Trace)

**Description:** Human-readable and machine-readable explanation of why a result/score/recommendation was produced.

**Operations**

- `GET   /api/v1/explanations`
- `GET   /api/v1/explanations/:id`
- `POST  /api/v1/explanations`

**Child collections**

- `GET /api/v1/explanations/:id/features` _(feature attributions, contributions)_
- `GET /api/v1/explanations/:id/rule-traces`

---

## 8️⃣ Model Invocation

**Description:** One execution of a specific model version with input/output and performance metadata.

**Operations**

- `GET   /api/v1/model-invocations`
- `GET   /api/v1/model-invocations/:id`
- `POST  /api/v1/model-invocations`

**Child collections**

- `GET /api/v1/model-invocations/:id/explanations`

---

## 9️⃣ Simulation Scenario

**Description:** “What-if” scenario definition (altered inputs, patient profiles, policies) for offline testing.

**Operations**

- `GET   /api/v1/simulation-scenarios`
- `GET   /api/v1/simulation-scenarios/:id`
- `POST  /api/v1/simulation-scenarios`
- `PATCH /api/v1/simulation-scenarios/:id`
- `DELETE /api/v1/simulation-scenarios/:id`

**Child collections**

- `GET /api/v1/simulation-scenarios/:id/simulation-runs`

---

## 🔟 Simulation Run

**Description:** Result of executing a scenario through rules/models (used for validation, tuning, QA).

**Operations**

- `GET   /api/v1/simulation-runs`
- `GET   /api/v1/simulation-runs/:id`
- `POST  /api/v1/simulation-runs`

**Child collections**

- `GET /api/v1/simulation-runs/:id/decision-results`
- `GET /api/v1/simulation-runs/:id/metrics`

---

## 1️⃣1️⃣ Decision Policy

**Description:** Configuration describing how to combine rules/models, thresholds, priorities, and channel behavior.

**Operations**

- `GET   /api/v1/decision-policies`
- `GET   /api/v1/decision-policies/:id`
- `POST  /api/v1/decision-policies`
- `PATCH /api/v1/decision-policies/:id`
- `DELETE /api/v1/decision-policies/:id`

**Child collections**

- `GET /api/v1/decision-policies/:id/threshold-profiles`

---

## 1️⃣2️⃣ Threshold Profile

**Description:** Reusable set of numeric / categorical thresholds (e.g., low/medium/high risk cutoffs, alert firing rules).

**Operations**

- `GET   /api/v1/threshold-profiles`
- `GET   /api/v1/threshold-profiles/:id`
- `POST  /api/v1/threshold-profiles`
- `PATCH /api/v1/threshold-profiles/:id`
- `DELETE /api/v1/threshold-profiles/:id`

---

## 1️⃣3️⃣ Decision Metric Snapshot

**Description:** Aggregated KPIs for a policy/model/decision line (acceptance rate, override rate, alert fatigue metrics).

**Operations**

- `GET   /api/v1/decision-metrics`
- `GET   /api/v1/decision-metrics/:id`
- `POST  /api/v1/decision-metrics`

---

## 1️⃣4️⃣ Experiment (A/B or Multi-arm Test)

**Description:** Experiment definition controlling which decision policy/model version is applied to which traffic slice.

**Operations**

- `GET   /api/v1/experiments`
- `GET   /api/v1/experiments/:id`
- `POST  /api/v1/experiments`
- `PATCH /api/v1/experiments/:id`
- `DELETE /api/v1/experiments/:id`

**Child collections**

- `GET /api/v1/experiments/:id/arms`
- `GET /api/v1/experiments/:id/results`

---

If you’re good with this, next we can:

- Do the same **extensive spec for (4) Workflow & Care Pathways Domain**, or
- Start turning Domains 1–3 into a **single OpenAPI YAML skeleton** that matches your MCP conventions.
