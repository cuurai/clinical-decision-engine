Here’s an **extensive pass for (4) Workflow & Care Pathways Domain**, in the same style as Domains 1–3 (REST + child collections).

---

## 1️⃣ Workflow Definition

**Description:** Abstract definition of a workflow (states, transitions, triggers) used across use cases.

**Operations**

- `GET   /api/v1/workflow-definitions`
- `GET   /api/v1/workflow-definitions/:id`
- `POST  /api/v1/workflow-definitions`
- `PATCH /api/v1/workflow-definitions/:id`
- `DELETE /api/v1/workflow-definitions/:id`

**Child collections**

- `GET /api/v1/workflow-definitions/:id/states`
- `GET /api/v1/workflow-definitions/:id/transitions`

---

## 2️⃣ Workflow Instance

**Description:** Runtime instance of a workflow for a specific patient / encounter / episode.

**Operations**

- `GET   /api/v1/workflow-instances`
- `GET   /api/v1/workflow-instances/:id`
- `POST  /api/v1/workflow-instances`
- `PATCH /api/v1/workflow-instances/:id` _(e.g., state change, pause, resume)_
- `DELETE /api/v1/workflow-instances/:id`

**Child collections**

- `GET /api/v1/workflow-instances/:id/tasks`
- `GET /api/v1/workflow-instances/:id/events`
- `GET /api/v1/workflow-instances/:id/audit-events`

---

## 3️⃣ Care Pathway Template

**Description:** Clinical care pathway definition (e.g., sepsis pathway, heart failure pathway).

**Operations**

- `GET   /api/v1/care-pathway-templates`
- `GET   /api/v1/care-pathway-templates/:id`
- `POST  /api/v1/care-pathway-templates`
- `PATCH /api/v1/care-pathway-templates/:id`
- `DELETE /api/v1/care-pathway-templates/:id`

**Child collections**

- `GET /api/v1/care-pathway-templates/:id/steps`
- `GET /api/v1/care-pathway-templates/:id/order-set-templates`

---

## 4️⃣ Care Plan

**Description:** Patient-specific care plan instantiated from guidelines/pathways (goals, activities, interventions).

**Operations**

- `GET   /api/v1/care-plans`
- `GET   /api/v1/care-plans/:id`
- `POST  /api/v1/care-plans`
- `PATCH /api/v1/care-plans/:id`
- `DELETE /api/v1/care-plans/:id`

**Child collections**

- `GET /api/v1/care-plans/:id/goals`
- `GET /api/v1/care-plans/:id/tasks`
- `GET /api/v1/care-plans/:id/checklists`

---

## 5️⃣ Episode of Care

**Description:** Longitudinal care episode linking encounters and workflows around a condition or admission.

**Operations**

- `GET   /api/v1/episodes-of-care`
- `GET   /api/v1/episodes-of-care/:id`
- `POST  /api/v1/episodes-of-care`
- `PATCH /api/v1/episodes-of-care/:id`
- `DELETE /api/v1/episodes-of-care/:id`

**Child collections**

- `GET /api/v1/episodes-of-care/:id/encounters`
- `GET /api/v1/episodes-of-care/:id/care-plans`
- `GET /api/v1/episodes-of-care/:id/workflow-instances`

---

## 6️⃣ Task

**Description:** Atomic unit of work to be performed (clinical or administrative).

**Operations**

- `GET   /api/v1/tasks`
- `GET   /api/v1/tasks/:id`
- `POST  /api/v1/tasks`
- `PATCH /api/v1/tasks/:id` _(status, due date, assignee, etc.)_
- `DELETE /api/v1/tasks/:id`

**Child collections**

- `GET /api/v1/tasks/:id/comments`
- `GET /api/v1/tasks/:id/audit-events`

---

## 7️⃣ Task Assignment

**Description:** Assignment record linking a task to a clinician, role, or team.

**Operations**

- `GET   /api/v1/task-assignments`
- `GET   /api/v1/task-assignments/:id`
- `POST  /api/v1/task-assignments`
- `PATCH /api/v1/task-assignments/:id`
- `DELETE /api/v1/task-assignments/:id`

---

## 8️⃣ Alert

**Description:** Clinical alert object generated by decision logic and routed to workflows/inboxes.

**Operations**

- `GET   /api/v1/alerts`
- `GET   /api/v1/alerts/:id`
- `POST  /api/v1/alerts`
- `PATCH /api/v1/alerts/:id` _(acknowledged, overridden, snoozed, etc.)_
- `DELETE /api/v1/alerts/:id`

**Child collections**

- `GET /api/v1/alerts/:id/explanations`
- `GET /api/v1/alerts/:id/audit-events`

---

## 9️⃣ Handoff

**Description:** Handoff event between teams/providers (e.g., ED → inpatient, day → night shift).

**Operations**

- `GET   /api/v1/handoffs`
- `GET   /api/v1/handoffs/:id`
- `POST  /api/v1/handoffs`
- `PATCH /api/v1/handoffs/:id`
- `DELETE /api/v1/handoffs/:id`

**Child collections**

- `GET /api/v1/handoffs/:id/tasks`

---

## 🔟 Checklist Template

**Description:** Reusable checklist definition (e.g., central line checklist, discharge checklist).

**Operations**

- `GET   /api/v1/checklist-templates`
- `GET   /api/v1/checklist-templates/:id`
- `POST  /api/v1/checklist-templates`
- `PATCH /api/v1/checklist-templates/:id`
- `DELETE /api/v1/checklist-templates/:id`

**Child collections**

- `GET /api/v1/checklist-templates/:id/items`

---

## 1️⃣1️⃣ Checklist Instance

**Description:** Patient/episode-specific checklist instantiated from a template.

**Operations**

- `GET   /api/v1/checklist-instances`
- `GET   /api/v1/checklist-instances/:id`
- `POST  /api/v1/checklist-instances`
- `PATCH /api/v1/checklist-instances/:id`
- `DELETE /api/v1/checklist-instances/:id`

**Child collections**

- `GET /api/v1/checklist-instances/:id/items`

---

## 1️⃣2️⃣ Escalation Policy

**Description:** Rules describing when and how tasks/alerts are escalated (time-based, severity-based).

**Operations**

- `GET   /api/v1/escalation-policies`
- `GET   /api/v1/escalation-policies/:id`
- `POST  /api/v1/escalation-policies`
- `PATCH /api/v1/escalation-policies/:id`
- `DELETE /api/v1/escalation-policies/:id`

**Child collections**

- `GET /api/v1/escalation-policies/:id/rules`

---

## 1️⃣3️⃣ Routing Rule

**Description:** Rules that determine which team/queue/inbox receives which task/alert.

**Operations**

- `GET   /api/v1/routing-rules`
- `GET   /api/v1/routing-rules/:id`
- `POST  /api/v1/routing-rules`
- `PATCH /api/v1/routing-rules/:id`
- `DELETE /api/v1/routing-rules/:id`

---

## 1️⃣4️⃣ Schedule Template

**Description:** Time-based patterns for recurring tasks/reminders (e.g., q4h vitals, daily labs).

**Operations**

- `GET   /api/v1/schedule-templates`
- `GET   /api/v1/schedule-templates/:id`
- `POST  /api/v1/schedule-templates`
- `PATCH /api/v1/schedule-templates/:id`
- `DELETE /api/v1/schedule-templates/:id`

---

## 1️⃣5️⃣ Work Queue

**Description:** Logical queue of tasks/alerts for a role or team (e.g., “ICU nurse queue”, “Cardiology consults”).

**Operations**

- `GET   /api/v1/work-queues`
- `GET   /api/v1/work-queues/:id`
- `POST  /api/v1/work-queues`
- `PATCH /api/v1/work-queues/:id`
- `DELETE /api/v1/work-queues/:id`

**Child collections**

- `GET /api/v1/work-queues/:id/tasks`
- `GET /api/v1/work-queues/:id/alerts`

---

If you’re good with this, next I can do **(5) Integration & Interoperability Domain** at the same level, or start turning Domains 1–4 into a **single OpenAPI/YAML skeleton** or **MCP capability list per domain**.
