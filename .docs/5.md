Here’s an **extensive Integration & Interoperability Domain** spec, same style as the others (clean REST + child collections).

---

## 1️⃣ External System

**Description:** Any external clinical/operational system we integrate with (EHR, LIS, RIS, PMS, HIE, payer, etc.).

**Operations**

- `GET   /api/v1/external-systems`
- `GET   /api/v1/external-systems/:id`
- `POST  /api/v1/external-systems`
- `PATCH /api/v1/external-systems/:id`
- `DELETE /api/v1/external-systems/:id`

**Child collections**

- `GET /api/v1/external-systems/:id/endpoints`
- `GET /api/v1/external-systems/:id/connections`
- `GET /api/v1/external-systems/:id/integration-jobs`

---

## 2️⃣ Connection (Endpoint Config)

**Description:** Concrete connection profile to an external system (base URL, credentials, FHIR/Hl7 profile, etc.).

**Operations**

- `GET   /api/v1/connections`
- `GET   /api/v1/connections/:id`
- `POST  /api/v1/connections`
- `PATCH /api/v1/connections/:id`
- `DELETE /api/v1/connections/:id`

**Child collections**

- `GET /api/v1/connections/:id/health-checks`
- `GET /api/v1/connections/:id/integration-jobs`

---

## 3️⃣ FHIR Bundle

**Description:** FHIR Bundle payloads received from or sent to external systems.

**Operations**

- `GET   /api/v1/fhir-bundles`
- `GET   /api/v1/fhir-bundles/:id`
- `POST  /api/v1/fhir-bundles` _(ingest or send)_
- `DELETE /api/v1/fhir-bundles/:id`

**Child collections**

- `GET /api/v1/fhir-bundles/:id/resources`

---

## 4️⃣ FHIR Mapping Profile

**Description:** Mapping rules between external FHIR profiles and the internal canonical model.

**Operations**

- `GET   /api/v1/fhir-mapping-profiles`
- `GET   /api/v1/fhir-mapping-profiles/:id`
- `POST  /api/v1/fhir-mapping-profiles`
- `PATCH /api/v1/fhir-mapping-profiles/:id`
- `DELETE /api/v1/fhir-mapping-profiles/:id`

**Child collections**

- `GET /api/v1/fhir-mapping-profiles/:id/rules`

---

## 5️⃣ HL7 Message

**Description:** Raw HL7 v2 messages (ADT, ORU, ORM, etc.) in and out.

**Operations**

- `GET   /api/v1/hl7-messages`
- `GET   /api/v1/hl7-messages/:id`
- `POST  /api/v1/hl7-messages` _(ingest)_
- `DELETE /api/v1/hl7-messages/:id`

**Child collections**

- `GET /api/v1/hl7-messages/:id/segments`
- `GET /api/v1/hl7-messages/:id/mapping-results`

---

## 6️⃣ HL7 Mapping Profile

**Description:** Mapping between HL7 segments/fields and internal canonical entities.

**Operations**

- `GET   /api/v1/hl7-mapping-profiles`
- `GET   /api/v1/hl7-mapping-profiles/:id`
- `POST  /api/v1/hl7-mapping-profiles`
- `PATCH /api/v1/hl7-mapping-profiles/:id`
- `DELETE /api/v1/hl7-mapping-profiles/:id`

**Child collections**

- `GET /api/v1/hl7-mapping-profiles/:id/rules`

---

## 7️⃣ Integration Job

**Description:** Unit of work for a sync/import/export operation (e.g., nightly patient sync, lab result feed).

**Operations**

- `GET   /api/v1/integration-jobs`
- `GET   /api/v1/integration-jobs/:id`
- `POST  /api/v1/integration-jobs`
- `PATCH /api/v1/integration-jobs/:id` _(status, retries, scheduling)_
- `DELETE /api/v1/integration-jobs/:id`

**Child collections**

- `GET /api/v1/integration-jobs/:id/runs`

---

## 8️⃣ Integration Run

**Description:** Execution instance of an integration job (with counts of records, errors, timings).

**Operations**

- `GET   /api/v1/integration-runs`
- `GET   /api/v1/integration-runs/:id`
- `POST  /api/v1/integration-runs`

**Child collections**

- `GET /api/v1/integration-runs/:id/logs`
- `GET /api/v1/integration-runs/:id/errors`

---

## 9️⃣ Data Import Batch

**Description:** Batch import of external data (CSV, NDJSON, bulk FHIR, etc.) into the canonical store.

**Operations**

- `GET   /api/v1/data-import-batches`
- `GET   /api/v1/data-import-batches/:id`
- `POST  /api/v1/data-import-batches`
- `PATCH /api/v1/data-import-batches/:id`
- `DELETE /api/v1/data-import-batches/:id`

**Child collections**

- `GET /api/v1/data-import-batches/:id/records`
- `GET /api/v1/data-import-batches/:id/errors`

---

## 🔟 Data Export Batch

**Description:** Batch export of data to target systems (for bulk reporting, registry submissions, etc.).

**Operations**

- `GET   /api/v1/data-export-batches`
- `GET   /api/v1/data-export-batches/:id`
- `POST  /api/v1/data-export-batches`
- `PATCH /api/v1/data-export-batches/:id`
- `DELETE /api/v1/data-export-batches/:id`

**Child collections**

- `GET /api/v1/data-export-batches/:id/files`
- `GET /api/v1/data-export-batches/:id/errors`

---

## 1️⃣1️⃣ Event Subscription

**Description:** Subscription to certain internal events (e.g., “lab result finalized”) for push to external systems.

**Operations**

- `GET   /api/v1/event-subscriptions`
- `GET   /api/v1/event-subscriptions/:id`
- `POST  /api/v1/event-subscriptions`
- `PATCH /api/v1/event-subscriptions/:id`
- `DELETE /api/v1/event-subscriptions/:id`

**Child collections**

- `GET /api/v1/event-subscriptions/:id/deliveries`

---

## 1️⃣2️⃣ Event Delivery

**Description:** Delivery attempt of an event to an external webhook/endpoint.

**Operations**

- `GET   /api/v1/event-deliveries`
- `GET   /api/v1/event-deliveries/:id`
- `POST  /api/v1/event-deliveries`
- `PATCH /api/v1/event-deliveries/:id` _(e.g., retry, mark failed)_

---

## 1️⃣3️⃣ API Client

**Description:** Client/application consuming your APIs (for auth, throttling, and usage tracking).

**Operations**

- `GET   /api/v1/api-clients`
- `GET   /api/v1/api-clients/:id`
- `POST  /api/v1/api-clients`
- `PATCH /api/v1/api-clients/:id`
- `DELETE /api/v1/api-clients/:id`

**Child collections**

- `GET /api/v1/api-clients/:id/credentials`
- `GET /api/v1/api-clients/:id/usage-metrics`

---

## 1️⃣4️⃣ API Credential

**Description:** Credential record (keys, secrets, certificates) associated with an API client.

**Operations**

- `GET   /api/v1/api-credentials`
- `GET   /api/v1/api-credentials/:id`
- `POST  /api/v1/api-credentials`
- `PATCH /api/v1/api-credentials/:id`
- `DELETE /api/v1/api-credentials/:id`

---

## 1️⃣5️⃣ Interface Error

**Description:** Individual error or validation failure during integration (message-level or record-level).

**Operations**

- `GET   /api/v1/interface-errors`
- `GET   /api/v1/interface-errors/:id`
- `POST  /api/v1/interface-errors`
- `PATCH /api/v1/interface-errors/:id` _(e.g., resolved, ignored, reprocessed)_

---

## 1️⃣6️⃣ Interface Health Check

**Description:** Health status snapshots of integrations/connections (latency, availability, last success/failure).

**Operations**

- `GET   /api/v1/interface-health-checks`
- `GET   /api/v1/interface-health-checks/:id`
- `POST  /api/v1/interface-health-checks`

---

If you want, next step could be:

- Turn **Domains 1–5** into a **MCP capability map** (per domain, per entity, per operation), or
- Start drafting a **single OpenAPI YAML skeleton** from these entities/paths.
