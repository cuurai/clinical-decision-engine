/**
 * Routes for patientEncounters
 *
 * Generated by Service Generator v1.0.0
 * Generator Version: 1.0.0
 * Domain: patient-clinical-data
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import type { FastifyInstance } from "fastify";
import { extractOrgId } from "../extract-org-id.js";
import type { Dependencies } from "../dependencies/patient-clinical-data.dependencies.js";
import { listPatientEncounters } from "@cuur-cde/core/patient-clinical-data/handlers/index.js";
export async function patientEncountersRoutes(
  fastify: FastifyInstance,
  deps: Dependencies
) {
  // GET /patients/{id}/encounters (nested route)
  fastify.get("/patients/:id/encounters", async (request, reply) => {
    const orgId = extractOrgId(request);
    const result = await listPatientEncounters(deps.patientEncounterRepo, orgId);
    return reply.code(200).send(result);
  });

  // GET /patient-encounters (flat route for dashboard)
  fastify.get("/patient-encounters", async (request, reply) => {
    const orgId = extractOrgId(request);
    const result = await listPatientEncounters(deps.patientEncounterRepo, orgId);
    return reply.code(200).send(result);
  });

}
