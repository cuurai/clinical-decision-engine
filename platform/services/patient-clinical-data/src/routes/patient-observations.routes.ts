/**
 * Routes for patientObservations
 *
 * Generated by Service Generator v1.0.0
 * Generator Version: 1.0.0
 * Domain: patient-clinical-data
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import type { FastifyInstance } from "fastify";
import { extractOrgId } from "../context/org-context.js";
import type { Dependencies } from "../dependencies/patient-clinical-data.dependencies.js";
import { listPatientObservations } from "@cuur-cde/core/patient-clinical-data/handlers/index.js";
export async function patientObservationsRoutes(
  fastify: FastifyInstance,
  deps: Dependencies
) {
  // GET /patients/{id}/observations (nested route)
  fastify.get("/patients/:id/observations", async (request, reply) => {
    const orgId = extractOrgId(request);
    const result = await listPatientObservations(deps.patientObservationRepo, orgId, request.query || {});
    return reply.code(200).send(result);
  });

  // GET /patient-observations (flat route for dashboard)
  fastify.get("/patient-observations", async (request, reply) => {
    const orgId = extractOrgId(request);
    const result = await listPatientObservations(deps.patientObservationRepo, orgId, request.query || {});
    return reply.code(200).send(result);
  });

}
