/**
 * DAO adapter for RuleSetClinicalRuleRepository
 *
 * Generated by Adapter Generator v1.0.0
 * Generator Version: 1.0.0
 * Domain: knowledge-evidence
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import type {
  OrgId,
  PaginatedResult,
  PaginationParams,
} from "@cuur-cde/core/_shared";
import type {
  RuleSetClinicalRuleRepository,
} from "@cuur-cde/core/knowledge-evidence/repositories/index.js";
import type {
  RuleSetClinicalRule,
} from "@cuur-cde/core/knowledge-evidence/types/index.js";
import type { DaoClient } from "../../_shared/src/dao-client.js";
import { NotFoundError, TransactionManager, handleDatabaseError } from "@cuur-cde/core/_shared";

const DEFAULT_LIMIT = 50;

export class DaoRuleSetClinicalRuleRepository implements RuleSetClinicalRuleRepository {
  private transactionManager: TransactionManager;

  constructor(private readonly dao: DaoClient) {
    this.transactionManager = new TransactionManager(dao);
  }

  async list(
    orgId: OrgId,
    params?: PaginationParams
  ): Promise<PaginatedResult<RuleSetClinicalRule>> {
    try {
      const limit = params?.limit ?? DEFAULT_LIMIT;

      const records = await this.dao.ruleSetClinicalRule.findMany({
        where: {
          orgId,
          deletedAt: null, // Soft delete filter - only return non-deleted records
        },
        orderBy: { createdAt: "desc" },
        take: limit,
        ...(params && 'cursor' in params && params.cursor ? {
          skip: 1,
          cursor: { id: params.cursor },
        } : {}),
      });

      return {
        items: records.map((r: any) => this.toDomain(r)),
        nextCursor: records.length === limit
          ? records[records.length - 1]?.id
          : undefined,
        prevCursor: undefined,
      };
    } catch (error) {
      handleDatabaseError(error);
      throw error;
    }
  }
  async findById(orgId: OrgId, id: string): Promise<RuleSetClinicalRule | null> {
    try {
      const record = await this.dao.ruleSetClinicalRule.findFirst({
        where: {
          orgId,
          id,
          deletedAt: null, // Soft delete filter - only return non-deleted records
        },
      });
      return record ? this.toDomain(record) : null;
    } catch (error) {
      handleDatabaseError(error);
      throw error;
    }
  }
  async get(orgId: OrgId, id: string): Promise<RuleSetClinicalRule | null> {
    const result = await this.findById(orgId, id);
    if (!result) {
      throw new NotFoundError("RuleSetClinicalRule", id);
    }
    return result;
  }
  private toDomain(model: any): RuleSetClinicalRule {
    return {
      ...model,
      createdAt: model.createdAt instanceof Date
        ? model.createdAt
        : new Date(model.createdAt),
      updatedAt: model.updatedAt instanceof Date
        ? model.updatedAt
        : model.updatedAt ? new Date(model.updatedAt) : undefined,
    } as RuleSetClinicalRule;
  }
}
